import { Story } from '@storybook/react';
// eslint-disable-next-line eliasxlii-plugin/layer-imports
import '@/app/styles/index.scss';

export const StyleDecorator = (StoryComponent: Story) => <StoryComponent />;
